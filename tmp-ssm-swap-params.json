{
  "commands": [
    "set -e",
    "sudo fallocate -l 2G /swapfile || sudo dd if=/dev/zero of=/swapfile bs=1M count=2048",
    "sudo chmod 600 /swapfile",
    "sudo mkswap /swapfile >/dev/null 2>&1 || true",
    "sudo swapon /swapfile || true",
    "grep -q '^/swapfile ' /etc/fstab || echo '/swapfile swap swap defaults 0 0' | sudo tee -a /etc/fstab >/dev/null",
    "echo 'vm.swappiness = 10' | sudo tee /etc/sysctl.d/99-song-to-lyrics.conf >/dev/null",
    "sudo sysctl -p /etc/sysctl.d/99-song-to-lyrics.conf >/dev/null",
    "free -h",
    "swapon --show",
    "df -h /"
  ]
}
